<odoo>
  <template id="page_chat_inline" name="GPT-5 Assistant Page (Inline)">
    <t t-call="web.layout">
      <t t-set="title">GPT-5 Assistant</t>
      <div class="o_container mt16">
        <div class="o_row">
          <div class="o_col col-12 col-lg-8">
            <div id="gpt5_chat_card" style="border:1px solid #ddd;border-radius:12px;padding:12px;max-height:70vh;display:flex;flex-direction:column;">
              <div id="gpt5_chat_messages" style="flex:1;overflow:auto;padding:8px;"></div>
              <div style="display:flex;gap:8px;">
                <input id="gpt5_chat_input" type="text" class="o_input" placeholder="اكتب سؤالك هنا… / Type your question…"/>
                <button id="gpt5_send_btn" class="btn btn-primary">Send</button>
              </div>
            </div>
          </div>
        </div>
      </div>
      <script type="text/javascript"><![CDATA[
      (function () {
          function renderMessage(role, content) {
              var messagesEl = document.getElementById("gpt5_chat_messages");
              var b = document.createElement("div");
              b.style.margin = "6px 0";
              b.style.padding = "8px 10px";
              b.style.borderRadius = "10px";
              b.style.background = role === "user" ? "#e9f5ff" : "#f6f6f6";
              b.textContent = content;
              messagesEl.appendChild(b);
              messagesEl.scrollTop = messagesEl.scrollHeight;
          }
          function mount() {
              var inputEl = document.getElementById("gpt5_chat_input");
              var sendBtn = document.getElementById("gpt5_send_btn");
              if (!inputEl || !sendBtn) return;
              var history = [];
              renderMessage("assistant", "مرحبًا! أنا مساعد Odoo الذكي. / Hi! I’m your Odoo assistant.");
              async function send() {
                  var text = (inputEl.value || "").trim();
                  if (!text) return;
                  inputEl.value = "";
                  renderMessage("user", text);
                  history.push({ role: "user", content: text });
                  var result = null;
                  try {
                      var resp = await fetch("/ai_assistant/chat", {
                          method: "POST",
                          headers: { "Content-Type": "application/json" },
                          body: JSON.stringify({ prompt: text, history: history }),
                      });
                      result = await resp.json();
                  } catch (e) {
                      result = { ok: false, error: (e && e.message) ? e.message : String(e) };
                  }
                  if (result && result.ok) {
                      renderMessage("assistant", result.reply || "(no reply)");
                      history.push({ role: "assistant", content: result.reply || "" });
                  } else {
                      renderMessage("assistant", "⚠ " + (result && result.error ? result.error : "Error"));
                  }
              }
              sendBtn.addEventListener("click", send);
              inputEl.addEventListener("keydown", function (e) {
                  if (e.key === "Enter") send();
              });
          }
          if (document.readyState === "loading") {
              document.addEventListener("DOMContentLoaded", mount);
          } else {
              mount();
          }
      })();
      ]]></script>
    </t>
  </template>
</odoo>
